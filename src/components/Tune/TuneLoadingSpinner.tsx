import React from 'react';
import { ISSpinner } from '../../assets/icons';

const TuneLoadingSpinner: React.FC = () => {
  return <ISSpinner className="h-[22px] animate-spin" />;
};

export default TuneLoadingSpinner;
